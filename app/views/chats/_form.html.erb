<div class="row mx-auto form-group">
  <%= form_with model: chat, data: {remote: true} do |f| %>   <!--rocal: false-->
    <%= f.text_field :message %>
    <%= f.hidden_field :room_id %>
  <% end %>
</div>